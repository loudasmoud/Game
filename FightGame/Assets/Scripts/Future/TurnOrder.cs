using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnOrder : MonoBehaviour {
    /// <summary>
    /// Attached to an object that is spawned at the start of battle.
    /// The object will be destroyed at the end of battle.
    /// 
    /// Not going with turn order for now.
    /// </summary>
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
