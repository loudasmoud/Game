#pragma strict

public var wisdom : int = 0;
public var myName : System.String = "noname";
public var strength : int = 0;
public var currentHP : int;
public var maxHP : int;

function Start () {
Debug.Log (strength);
Debug.Log (wisdom);
Debug.Log (myName);
currentHP = maxHP;
}

function Update () {
    
}